services:
  postgres-database:
    image: postgres:15
    container_name: bank-api-database
    restart: always
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: